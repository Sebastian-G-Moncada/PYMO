<!-- resources/views/livewire/agregar-insumo-form.blade.php -->

{{-- <div>
    <button wire:click="mostrarFormulario" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
        Agregar Insumo
    </button>

    @if ($mostrarFormulario)
        <form wire:submit.prevent="agregarInsumo">
            <div class="mb-4">
                <label for="nombre">Nombre del Insumo</label>
                <input type="text" wire:model="nombre" id="nombre">
            </div>
            <div class="mb-4">
                <label for="cantidad">Cantidad</label>
                <input type="number" wire:model="cantidad" id="cantidad">
            </div>
            <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                Guardar
            </button>
        </form>
    @endif
</div> --}}

